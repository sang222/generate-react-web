# Dependency Policy
- Keep dependencies minimal and compatible with the configured stacks.
- Do not add optional libraries unless the story explicitly requires them.
- Prefer built-in platform or framework capabilities before introducing new packages.
