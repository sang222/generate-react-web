# Existing Project Examples

## Good pattern
- Reuse the delivered app shell.
- Add a new catalog module beside current routes.
- Add new backend controller/service classes without touching auth or checkout.

## Bad pattern
- Regenerate the full project tree.
- Rewrite the shared layout to fit one story.
- Move existing modules without a change request.
- Replace baseline contracts when additive changes are enough.
