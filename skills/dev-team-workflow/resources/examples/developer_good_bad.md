# Developer Good / Bad Patterns

Good:
- returns minimal valid JSON with only required files
- patches baseline safely and keeps imports/build realistic

Bad:
- returns prose or markdown
- rewrites unrelated files
- invents a different backend or build tool
