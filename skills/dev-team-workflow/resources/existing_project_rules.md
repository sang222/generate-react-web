# Existing Project Rules

Use when the workflow is changing an existing project. Preserve current conventions, limit touched files, and prefer targeted updates over broad rewrites. Brownfield support is partial in this codebase: touched files still return full contents.
