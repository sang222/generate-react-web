# New Project Rules

Use when the workflow is generating a React + Vite app from scratch. Prefer the minimum runnable file set first, then layer features. Keep dependency count low and file tree concrete.
