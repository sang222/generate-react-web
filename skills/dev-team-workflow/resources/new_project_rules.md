# New Project Rules

Use when the workflow is generating a new project from scratch. Default target is React + Vite for frontend, Java + Spring Boot with Gradle for backend, and PostgreSQL + JPA for database unless config overrides it. Prefer the minimum runnable file set first, then layer features. Keep dependency count low and file tree concrete.
