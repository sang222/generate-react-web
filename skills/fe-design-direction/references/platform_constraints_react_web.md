# React Web Constraints

- Use existing routing and styling conventions.
- Prefer component composition over page-sized monoliths.
- Keep tokens compatible with CSS variables or existing Tailwind/theme config.
