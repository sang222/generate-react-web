# When To Use

- Use before FE implementation for any story with UI/page/component changes.
- Use when brief says things like "make it beautiful", "modern", "smooth", or "landing page".
- Use for existing_project UI work to preserve conventions before code is generated.
