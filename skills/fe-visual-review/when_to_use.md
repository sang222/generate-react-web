# When To Use

- Use after FE implementation and build/screenshot check.
- Use before integration QA for any frontend lane output.
- Use during retries when UI looks generic, inconsistent, or off-direction.
