# FE UI Implementation Output Artifact

- JSON object `{ files: [...], touched_boundary: {...} }`.
- `files` contains production source files only.
- `touched_boundary` is mandatory for brownfield and existing project safety.
