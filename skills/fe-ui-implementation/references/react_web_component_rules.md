# React Web Component Rules

- Keep components small and named by domain purpose.
- Prefer existing route and state patterns.
- Scope styles locally or through existing Tailwind/theme conventions.
- Avoid global CSS unless the project already uses it.
