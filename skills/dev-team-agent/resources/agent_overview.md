# Agent Overview

This skill is the conversational front door of the module. It brings persona, memory, and routing. It should not absorb the whole procedural workflow into its own prompt; instead it calls the workflow when structured execution is needed.
